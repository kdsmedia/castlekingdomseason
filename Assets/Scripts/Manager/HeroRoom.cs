using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// HeroRoom — placeholder MonoBehaviour attached to the hero room GameObject.
/// Start() and Update() are intentionally empty; game logic for this room
/// is driven externally (e.g. by Manager or UI scripts).
/// Implement room-specific behaviour here when needed.
/// </summary>
public class HeroRoom : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
